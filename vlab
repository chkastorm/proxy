function FindProxyForURL(url, host) {
    var regexpr = /^https?:\/\/.*\.lab-services\.ca.*$/;
    if (regexpr.text(host)) {
        return "PROXY vlabproxy1.ms.bell.ca:8080; PROXY vlabproxy2.ms.bell.ca:8080"; 
    }
    else {
    return "DIRECT";
    }
}
